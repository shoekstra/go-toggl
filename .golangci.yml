run:
  timeout: 5m
  tests: true

linters:
  enable:
    - gofmt
    - goimports
    - govet
    - lll
    - misspell
    - namedreturn
    - revive
    - ineffassign
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - lll
